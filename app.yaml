runtime: python313
entrypoint: gunicorn -b :$PORT main:application

env_variables:
  GAE_USE_SUBPROCESS: 0
handlers:
- url: /js
  static_dir: js  

- url: /css
  static_dir: css  
  
- url: /img
  static_dir: img  

- url: /.*
  script: auto
